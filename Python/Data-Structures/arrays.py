# use arrays if you have a larrrge sequence of numbers

from array import array
# typecode for the type of items in the array
# check google for typecode

numbers = array("i", [1, 2, 3])
print(numbers[0])
